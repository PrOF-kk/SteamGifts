<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Note: The placeholder should be set both
         as the ImageView xml src and Picasso placeholder(),
         since the view is reused by RecyclerViews and the
         current drawable is not reset back to the xml defined one otherwise -->
    <item android:width="184dp" android:height="69dp">
        <color android:color="#222222" />
    </item>
    <item
        android:width="69dp"
        android:height="69dp"
        android:drawable="@drawable/sgwhite"
        android:gravity="center" />
</layer-list>